{
  "buildCommand": "cd 'Ascend App' && npm install && npm run build",
  "outputDirectory": "Ascend App/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
